# Server runtime for stadtanalyse.srikarkodi.dev (no Docker, no ML).
# sklearn/scipy/xgboost intentionally dropped: the trained model is a sklearn
# Pipeline, so the ML panel would need the ~450MB heavy deps. Everything else
# (KPIs, delays, routes, hotspots, weather, live snapshot) works without them.
fastapi==0.115.4
uvicorn[standard]==0.32.0
psycopg2-binary==2.9.10
kafka-python==2.0.2
pydantic==2.9.2
pydantic-settings==2.6.0
prometheus-client==0.21.0
joblib==1.4.2
numpy==1.26.4
pandas==2.2.3
duckdb>=0.10.0
